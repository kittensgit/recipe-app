import { FC } from 'react';

import Search from 'components/search/Search';

const Home: FC = () => {
    return (
        <div className="container">
            <Search />
        </div>
    );
};

export default Home;
